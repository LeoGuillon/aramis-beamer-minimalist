\RequirePackage{ifthen}

\mode<presentation>

\newboolean{nexa}
\setboolean{nexa}{false}

\DeclareOptionBeamer{nexa}{\setboolean{nexa}{true}}


\ProcessOptionsBeamer

\ifLuaTeX
  \RequirePackage{fontspec}
  \ifthenelse{\boolean{nexa}}{
    \setsansfont{Nexa}[
      Path = fonts/,
      UprightFont = *Light,
      BoldFont = *Bold,
      ItalicFont = *LightItalic,
      BoldItalicFont = *BoldItalic,
      Extension = .otf
    ]
  }{
    \setsansfont{Trebuchet MS}
  }
\fi

\ifPDFTeX
  \RequirePackage[T1]{fontenc}
  \RequirePackage{times}
\fi

%  if you want to customize the various font sizes
% \renewcommand\footnotesize{\fontsize{8pt}{10pt}\selectfont}
% \renewcommand\small{\fontsize{10pt}{12pt}\selectfont}
% \renewcommand\normalsize{\fontsize{12pt}{14pt}\selectfont}
% \renewcommand\large{\fontsize{14pt}{16pt}\selectfont}
% \renewcommand\Large{\fontsize{16pt}{18pt}\selectfont}


\setbeamerfont{normal text}{family = \sffamily, size = \normalsize, series = \mdseries}

\setbeamerfont{all titles}{parent = normal text}
\setbeamerfont{title}{parent = all titles,
  size = \Large,
  series = \bfseries,
  shape = \scshape}
\setbeamerfont{frametitle}{parent = all titles, size = \large, series = \bfseries}
\setbeamerfont{subtitle}{parent = all titles, shape = \itshape}

\setbeamerfont{author}{parent = normal text, size = \small}
\setbeamerfont{institute}{parent = normal text, size = \small}
\setbeamerfont{date}{parent = normal text, size = \small}

\setbeamerfont{section in toc}{parent = normal text, series = \bfseries}

\setbeamerfont{framenumber}{parent = normal text, size = \footnotesize}

\setbeamerfont{caption}{parent=normal text, size=\footnotesize}
\setbeamerfont{caption name}{parent=caption, series = \bfseries}

\mode<all>
